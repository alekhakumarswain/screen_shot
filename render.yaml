services:
  - type: web
    name: flask-playwright-snapshot
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: "/root/.cache/ms-playwright"
      - key: PORT
        value: "10000"

